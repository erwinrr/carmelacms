<nav class="form-nav">
  <div class="nav nav-tabs" id="nav-tab" role="tablist">
    <a class="nav-item nav-link active" id="nav-sales-tab" data-toggle="tab" href="#nav-sales" role="tab" aria-controls="nav-sales" aria-selected="true">Sales</a>
    <a class="nav-item nav-link" id="nav-service-tab" data-toggle="tab" href="#nav-service" role="tab" aria-controls="nav-service" aria-selected="false">Service</a>
  </div>
</nav>
<div class="tab-content" id="nav-tabContent">
  <div class="tab-pane fade show active" id="nav-sales" role="tabpanel" aria-labelledby="nav-sales-tab">
    <div class="col-12">
      <div class="card card-table">
        <div class="card-header">
          <div class="tools"></div>
          <div class="title"></div>
        </div>
        <div class="card-body table-responsive">
          <table class="table table-striped table-borderless">
            <thead>
              <tr>
                <th>Date</th>
                <th>Customer</th>
                <th>Time</th>
              </tr>
            </thead>
            <tbody class="no-border-x">
              <% if @sales_forms.empty? %>
                <tr>
                  <td colspan="3">
                    No forms have been submitted yet..
                  </td>
                </tr>
              <% end %>
              <% @sales_forms.each do |form| %>
                <tr>
                  <td><%= link_to form.created_at.strftime("%b %d, %Y %I:%M %p"), organization_basic_form_path(@organization, form) %></td>
                  <td><%= form.user.full_name %></td>
                  <td><%= time_ago_in_words(form.created_at) %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
  <div class="tab-pane fade" id="nav-service" role="tabpanel" aria-labelledby="nav-service-tab">
    <div class="col-12">
      <div class="card card-table">
        <div class="card-header">
          <div class="tools"></div>
          <div class="title"></div>
        </div>
        <div class="card-body table-responsive">
          <table class="table table-striped table-borderless">
            <thead>
              <tr>
                <th>Date</th>
                <th>Customer</th>
                <th>Time</th>
              </tr>
            </thead>
            <tbody class="no-border-x">
              <% if @service_forms.empty? %>
                <tr>
                  <td colspan="3">
                    No forms have been submitted yet..
                  </td>
                </tr>
              <% end %>
              <% @service_forms.each do |form| %>
                <tr>
                  <td><%= link_to form.created_at.strftime("%b %d, %Y %I:%M %p"), organization_basic_form_path(@organization, form) %></td>
                  <td><%= form.user.full_name %></td>
                  <td><%= time_ago_in_words(form.created_at) %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>